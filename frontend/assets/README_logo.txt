Replace assets/logo.png with the Servest logo you uploaded in the chat. The frontend expects the file at `frontend/assets/logo.png`.

To add the logo, upload the file to the repository path `frontend/assets/logo.png` or when running locally, put the image in that folder.
